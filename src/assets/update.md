For deploying. 
Pull request into main.
Do a git pull. 
npm run build. 
npm run deploy. 
Published new version to ghpages


Skills updates. 

SkillIcon.jsx
─────────────────────────────────────────────────────────────
Single source of truth for every skill icon.
Adding new skils: 
- Import the icon from react-icons = https://react-icons.github.io/react-icons into the SkillsIcon.jsx file. 
- Add a new entry to ICON_MAP below using the skill name as the key
- Add the skill to skills.json with the matching name + category
- Key must exactly match the "name" field in skills.json

footer is the attempt. 
Reverting back to origina;l
